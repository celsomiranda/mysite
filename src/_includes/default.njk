<!DOCTYPE html>
<html lang="pt-PT">
  <head>
    <!-- https://github.com/joshbuchea/HEAD -->

    <!-- Recommended -->
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    <!-- Meta -->
    <meta name="description" content="{{ metadata.description }}"/>
    <meta name="generator" content="11ty"/>

    <!-- Icons -->
    <!-- https://favicon.io/ -->
    <link rel="apple-touch-icon" sizes="180x180" href="{{ '/apple-touch-icon.png' | url }}"/>
    <link rel="icon" type="image/png" sizes="32x32" href="{{ '/favicon-32x32.png' | url }}"/>
    <link rel="icon" type="image/png" sizes="16x16" href="{{ '/favicon-16x16.png' | url }}"/>
    <link rel="manifest" href="{{ '/site.webmanifest' | url }}"/>

    <title>{{ title or metadata.title }}</title>

    <link rel="stylesheet" href="{{ '/site.css' | url }}"/>
  </head>

  <body class="max-w-full mx-auto border-green-500 border-[1rem] bg-zinc-800 text-slate-50 font-mono">
    <div class="max-w-5xl p-6 mx-auto">
      <header class="flex flex-row items-center">
        <img class="hidden md:block md:w-28 md:h-28" src="img/avatar.png" alt="my avatar is a french buldog with a yellow hoodie">
        <div class="flex flex-col flex-grow md:ml-6">
        <a class="text-2xl font-bold md:text-4xl" href="/">{{ title or metadata.title }}<span class="text-green-500">.</span></a>
        <nav class="flex flex-row">
          <a class="my-1 mr-4" href="/">Tags</a>
          <a class="my-1 mr-4" href="/">Início</a>
          <a class="my-1 mr-4" href="/">Sobre</a>
          <a class="my-1" href="/">Contactos</a>
        </nav>
        </div>
      </header>
      {{ content | safe }}
      <footer>
        <hr class="mt-6 mb-2 border-t-4 border-blue-700 border-dotted">
        <p>&copy; 2022 Celso Miranda</p>
      </footer>
    </div>
  </body>
</html>
